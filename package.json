{
  "name": "flight",
  "version": "1.0.0",
  "devDependencies": {
    "karma": "~0.8.5",
    "loadbuilder": ">=0.4.2"
  },
  "scripts": {
    "test": "./node_modules/.bin/karma start --browsers PhantomJS --single-run"
  }
}
